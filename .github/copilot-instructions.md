# Project Instructions

This repository is a MongoDB CRUD mini project.

- Frontend: React with Material UI.
- Backend: Node.js, Express, Mongoose.
- Database: MongoDB.
- Main entity: student project records.

Prefer clear, small changes and keep the app runnable with `npm start` in both `backend` and `frontend`.
